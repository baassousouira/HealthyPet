<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- the cat image -->
    <item android:drawable="@drawable/turtle"
        android:width="80dp"
        android:height="80dp" />

    <!-- Background shape with orange border -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="0.5dp"
                android:color="#FFA500" /> <!-- Orange color for the border -->
            <corners android:radius="20dp" /> <!-- Border radius -->
        </shape>
    </item>
</layer-list>